{
  "name": "shared-texture-demo",
  "version": "1.0.0",
  "main": "src/main.js",
  "scripts": {
    "build": "node-gyp rebuild --target=$(node -e \"console.log(require('./node_modules/electron/package.json').version)\") --arch=arm64 --dist-url=https://electronjs.org/headers",
    "start": "ELECTRON_RUN_AS_NODE= electron ."
  },
  "dependencies": {
    "node-addon-api": "^8.3.1"
  },
  "devDependencies": {
    "electron": "^43.1.1",
    "electron-rebuild": "^3.2.9"
  }
}
